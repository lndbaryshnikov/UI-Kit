include ../../ui-kit-components/slider/slider
include ../block-sign/block-sign

mixin sliders(itemOptions)
  .sliders
    +block-sign('sliders')

    .sliders__container
      each options in itemOptions
        .sliders__slider
          +slider(options)
