mixin dropdown(options)
  select.dropdown.js-dropdown
    option.dropdown__option(selected, disabled) Choose An Option
    
    each value in options
      option.dropdown__option(value=value)= value
