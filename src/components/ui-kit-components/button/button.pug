mixin button({ size, color, content, width, inverse = false })
  -
    const backSize = width ? `button_size_${size} button_width_${width}` : `button_size_${size}`;
    const buttonColor = inverse ? `${color}-inverse` : color;
    const rippleColor = inverse ? color : 'white';

  .button(class=[backSize, `button_color_${color}`])
    button.button__button-element(
      type="submit",
      class=[`button__button-element_size_${size}`, `button__button-element_color_${buttonColor}`, "js-ripple"],
      data-ripple-color=rippleColor
    )= content
