mixin toggle(isChecked)
    label.toggle
        input(type="checkbox" checked=isChecked)
        span.toggle__slider
        div(class="toggle__sign toggle__sign_off") off
        div(class="toggle__sign toggle__sign_on") on
